# Development Environment - Uses Mock Data

# Enable mock data mode (no real API calls)
VITE_USE_MOCK_DATA=true

# Mock API response delay in milliseconds (simulates network latency)
VITE_MOCK_DELAY=800

# Backend API base URL (not used when VITE_USE_MOCK_DATA=true)
VITE_API_BASE_URL=http://localhost:8133
