tracker-ops:
  host: localhost
  deluge:
    host: localhost
    port: 8112
    download-folder: /Downloads/running

server:
  port: 8133

---

spring:
  datasource:
    url: jdbc:h2:mem:db
  jpa:
    hibernate.ddl-auto: create-drop
    show-sql: true
  config:
    activate:
      on-profile: stats